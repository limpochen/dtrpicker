/**
 * 两种渲染模式（SVG / HTML+CSS）的共用尺寸常量。
 *
 * 网格布局常量和今日按钮尺寸常量集中在此处定义，
 * 避免两套渲染器各自维护不同的硬编码值。
 *
 * @const {Object}
 */
export const DIM = {

  // ══════════════════════════════════════════════════════════════
  //  网格布局常量
  // ══════════════════════════════════════════════════════════════

  /** 单元格高度（px） */
  CELL_H: 36,

  /** 单元格宽度（px），按场景分三种取值 */
  CELL_W: {
    /** 桌面端宽度 */
    DESKTOP: 40,
    /** 移动端无时间模式宽度 */
    MOBILE: 36,
    /** 移动端+时间模式宽度 */
    MOBILE_TIME: 30,
  },
  /** 网格间隙（px） */
  GAP: 1,
  /** 侧边栏列数（年 + 月） */
  SIDEBAR_COLS: 2,
  /** 日期列起始索引（在侧边栏之后） */
  DATE_COL_START: 2,
  /** 日期列数（一周 7 天） */
  DATE_COLS: 7,
  /** 时间列起始索引 */
  TIME_COL_START: 9,
  /** 时间列数（时 + 分） */
  TIME_COLS: 2,
  /** 表头行号 */
  HEADER_ROW: 0,
  /** 日历区域可见行数（视口内） */
  VISIBLE_DATE_ROWS: 8,
  /** 上下缓冲行数（虚拟滚动用） */
  BUFFER_ROWS: 12,

  /** 版本号 */
  VERSION: 'v2.1.0',
  /** 是否在容器左下角显示版本号 */
  SHOW_VERSION: true,

  /**
   * 年月显示方式。
   * - 'column'：以当前方式单独列显示（年列 + 月列）
   * - 'watermark'：在日历日期区以大字号水印方式叠加显示
   */
  YEAR_MONTH_MODE: 'watermark',

  // ══════════════════════════════════════════════════════════════
  //  回到今日按钮常量
  // ══════════════════════════════════════════════════════════════

  TODAY_BTN: {
    /** 按钮外径（px） */
    SIZE: 24,
    /** 按钮距离左下角的边距（px） */
    MARGIN: 16,
    /** 外圈圆环半径（px） */
    RING_R: 7,
    /** 中心圆点半径（px） */
    DOT_R: 3,
    /** 外圈圆环描边宽度（px） */
    STROKE_W: 1.5,
    /** 阴影水平偏移（px，正值向右） */
    SHADOW_X: 2,
    /** 阴影垂直偏移（px，正值向下） */
    SHADOW_Y: 2,
  },
};
